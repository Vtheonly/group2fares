services:
  builder:
    build: .
    volumes:
      # Mount local folders for input/output
      - ./input:/factory_builder/input
      - ./output:/factory_builder/output
      - ./cache:/app/cache
    environment:
      # Override API URL via environment variable if needed
      - API_URL=${API_URL:-https://prettyish-melanie-aurous.ngrok-free.dev/generate}
