services:
  architect:
    build: .
    container_name: factory_architect
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - INPUT_FILE=/app/data/input.json
      - OUTPUT_DIR=/app/data/output
